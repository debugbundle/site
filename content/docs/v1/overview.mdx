---
title: "DebugBundle v1 overview"
description: "DebugBundle captures runtime incidents, assembles deterministic debugging bundles, and exposes the same workflows through API, CLI, MCP, and the public docs surface."
---

<SurfaceGrid>
  <SurfaceCard title="Capture">
    SDKs and local-first ingestion paths produce normalized event data without requiring the web UI.
  </SurfaceCard>
  <SurfaceCard title="Assemble">
    Workers transform incidents into bundles and reproduction artifacts that are designed for agent consumption.
  </SurfaceCard>
  <SurfaceCard title="Retrieve">
    The same capabilities remain available through API, CLI, MCP, and now the public docs surface.
  </SurfaceCard>
</SurfaceGrid>